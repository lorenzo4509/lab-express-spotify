<!DOCTYPE html>
<html>
<head>
    <title>Tracks</title>
</head>
<body>
    <h1>Tracks</h1>
    {{#if tracks.length}}
        <ul>
            {{#each tracks}}
                <li>
                    <audio controls>
                        <source src="{{ preview_url }}" type="audio/mpeg">
                        Your browser does not support the audio element.
                    </audio>
                    <h3>{{ name }}</h3>
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No tracks found.</p>
    {{/if}}
</body>
</html>
